export const theme = {
  colors: {
    // Brand / Identity
    primary: "#8C1D18",      // Deep Tunisian red (not too bright)
    secondary: "#1F6F43",    // Dark Islamic green
    accent: "#C9A24D",       // Warm gold (Zitouna / heritage)

    // Backgrounds
    background: "#F9F7F3",   // Soft parchment / mosque stone
    surface: "#FFFFFF",     // Cards, modals

    // Text
    text: "#1C1C1C",         // Strong readable black
    muted: "#6B6B6B",        // Secondary text

    // Status
    success: "#2E7D32",      // Confirmation / verified
    warning: "#D4A017",      // Gold warning (instead of orange)
    danger: "#B91C1C",       // Deep red error

    // UI Details
    border: "#E5E2DC",
    overlay: "rgba(0,0,0,0.4)",
  },

  spacing: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    xxl: 32,
  },

  radius: {
    sm: 6,
    md: 10,
    lg: 16,
    xl: 24,
  },

  typography: {
    h1: 28,
    h2: 22,
    h3: 18,
    body: 16,
    small: 14,
    caption: 12,
  },
};
